<%= form_with(model: project, local: true) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, id: :project_title, placeholder: "Title", class: "form-control" %>
  </div>

  <br />

  <div class="form-group">
    <%= form.cktext_area :description, id: :project_description %>
  </div>

  <div class="form-group">
    <%= form.hidden_field :user_id, id: :project_user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit "Save It!", class: "btn btn-primary" %>
  </div>
<% end %>
